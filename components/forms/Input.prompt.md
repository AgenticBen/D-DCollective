Text, email and number entry inside a Field.

```jsx
<Input id="org" placeholder="Organization name" />
```

Reuse the exported `inputSkin` object when you need the same treatment on a non-input element.
