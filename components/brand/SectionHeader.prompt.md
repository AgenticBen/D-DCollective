Opens a page or section: optional eyebrow, heading, gradient rule, muted lede.

```jsx
<SectionHeader
  eyebrow="How we work"
  title="Mission, vision, and values"
  lede="Draft language for the site, pulled from the Praxis Capital Fellowship manifesto."
/>
```

Use `level={1}` once per page.
