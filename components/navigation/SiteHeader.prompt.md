Site-wide masthead. Nav items are plain text; the active item gets a teal underline.

```jsx
<SiteHeader items={pages} active={page} onNavigate={setPage} action={<Button size="sm">Connect</Button>} />
```
