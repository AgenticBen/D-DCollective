Renders the supplied D+D Collective logo artwork — use for every appearance of the brand mark.

```jsx
<Logo variant="lockup" height={36} assetBase="../../assets" />
```

Variants: `lockup` / `mark` on light grounds, `lockup-dark` / `mark-dark` on near-black, and the `-transparent` pair when the mark sits on a coloured surface. Do not recolour, outline, or set the wordmark in live type.
