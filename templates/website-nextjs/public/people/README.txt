Portraits are no longer served from here.

Everyone on /changemakers uploads their own photograph through /profile, and it
is stored in the Supabase `people-photos` bucket against their row in `people`.
That is what keeps a face on the site tied to the consent that put it there.

This directory is kept only so an existing deployment does not 404 on a path it
already serves. Do not add new portraits to it.
