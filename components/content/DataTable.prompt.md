Listing table — the primary way D+D presents partners and portfolio.

```jsx
<DataTable columns={['Organization','Region','Posture']} rows={[['Example Fund I','East Africa',<Pill>At market</Pill>]]} />
```

Rows never zebra-stripe; the last row drops its border.
